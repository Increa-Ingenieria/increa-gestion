// package.json
{
  "name": "increa-gestion",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "next build && electron-builder"
  },
  "build": {
    "appId": "com.increa.gestion",
    "win": {
      "target": "nsis",
      "icon": "assets/icon.ico"
    }
  },
  "devDependencies": {
    "electron": "^21.0.0",
    "electron-builder": "^23.0.0"
  }
}
